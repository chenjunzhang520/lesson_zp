# es6+
2015年作为分水岭， 之前 js 有好的一方面（单线程，简单）， 不好的一方面（变量提升，class 关键词都没有）
企业级开发语言、别的语言有的他要有， 适合大型项目开发。

- 优雅
- 之前不支持的，作为大型语言需要有的
## 解构

- 一次性声明多个变量
- 解构了数组，甚至还可以解构嵌套数组。
- 解构对象，
- 左右两边一致

## reset 运算符 ...

## 对象的属性简写

## 模板字符串
${} 插值表达式  运行， 返回值

## for of 循环
  简单循环，语义更好 可读性好
  相对于计数循环

## bigint 大数
  - 新的简单数据类型
  - 后面加n


## ** 指数运算 es7 2016

## 函数的默认参数