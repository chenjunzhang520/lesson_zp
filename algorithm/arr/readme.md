- 数据结构要搞哪些？
  ## 线性数据结构
  - 数组  常用、好用、连续内存
  - 栈  先进后出 FILO
  - 队列  先进先出 FIFO
  - 链表  离散
  ## 非线性数据结构
  - 树 (二叉树)

## 数组 **开箱即用**的数据结构
- 数组的创建 
  - 了解数组的每一项
  const arr = [1, 2, 3, 4, 5, 6];
  - 不知道内部的元素情况的? 初始化一个数组？
    new Array(10).fill(0);
    - 要多了浪费
    - 要少了怎么办？ 
      二次申请内存空间 (10) 开销比较大的
      JS 支持动态扩容 
      搬家 
      考虑动态性时候，数组是链表差的
      少数数据的情况下，数组是更优秀的，链表节点的实例化多个指针

## 遍历数组
  arr[index]  O(1)
  