# Todos 任务清单 VUE开发思路

- 响应式数据驱动的界面开发
- v-if/v-for/v-model
  composition API
  @keydown.enter
  :class
  computed
  VUE 开发方式 简单，高效，好上手。

- 传统做法
  先找到目标元素，然后对其做修改(DOM)
- VUE 做法
  不在需要思考页面的元素怎么操作，而是要思考数据是怎么变化的。
- v-for  循环输出
  v- 指令
- computed 计算属性
  - 性能更好，依赖的响应式数据变化才会重新计算